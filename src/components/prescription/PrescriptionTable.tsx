import type { Prescription } from '../../types';

export default function PrescriptionTable({ prescription }: { prescription: Prescription }) {
  const rows = [['SPH', prescription.rightEye.sph, prescription.leftEye.sph], ['CYL', prescription.rightEye.cyl, prescription.leftEye.cyl], ['AXIS', prescription.rightEye.axis ? `${prescription.rightEye.axis}°` : '', prescription.leftEye.axis ? `${prescription.leftEye.axis}°` : ''], ['ADD', prescription.rightEye.add, prescription.leftEye.add]];
  return <div className="overflow-hidden rounded-[20px] border border-white/10"><table className="w-full text-left text-sm"><thead className="bg-[#1B1F2B] text-xs uppercase tracking-[0.2em] text-slate-400"><tr><th className="px-4 py-3">Power</th><th className="px-4 py-3">Right eye (OD)</th><th className="px-4 py-3">Left eye (OS)</th></tr></thead><tbody>{rows.map(([label, right, left]) => <tr key={label} className="border-t border-white/10"><th className="px-4 py-3 font-medium text-slate-300">{label}</th><td className="px-4 py-3 text-white">{right || 'Not entered'}</td><td className="px-4 py-3 text-white">{left || 'Not entered'}</td></tr>)}<tr className="border-t border-white/10"><th className="px-4 py-3 font-medium text-slate-300">PD</th><td colSpan={2} className="px-4 py-3 text-white">{prescription.pd ? `${prescription.pd} mm` : 'Not entered'}</td></tr></tbody></table></div>;
}
