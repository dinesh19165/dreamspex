import { useState } from 'react';
import { ArrowRight, CheckCircle, Heart, Package, Plus, Settings, UserRound, WalletCards } from 'lucide-react';
import { Link } from 'react-router-dom';
import Navbar from '../components/layout/Navbar';
import Footer from '../components/layout/Footer';
import SectionTitle from '../components/ui/SectionTitle';
import Button from '../components/ui/Button';
import { useShop } from '../context/ShopContext';

function AccountShell({ children }: { children: React.ReactNode }) {
  return <div className="min-h-screen bg-[#050816] text-slate-100"><Navbar /><main className="mx-auto max-w-7xl px-4 py-16 sm:px-6 lg:px-8">{children}</main><Footer /></div>;
}

function EmptyState({ icon: Icon, title, copy, to, action }: { icon: typeof Package; title: string; copy: string; to: string; action: string }) {
  return <div className="mt-8 rounded-[24px] border border-white/10 bg-white/5 p-10 text-center backdrop-blur-xl"><Icon className="mx-auto h-10 w-10 text-cyan-300" /><h2 className="mt-5 text-xl font-semibold text-white">{title}</h2><p className="mx-auto mt-3 max-w-md text-sm leading-7 text-slate-400">{copy}</p><Link to={to} className="mt-6 inline-flex items-center gap-2 rounded-full bg-gradient-to-r from-cyan-400 to-blue-600 px-5 py-3 text-sm font-semibold text-white">{action}<ArrowRight className="h-4 w-4" /></Link></div>;
}

export function DashboardPage() {
  const { wishlist, cart } = useShop();
  return <AccountShell><SectionTitle eyebrow="My account" title="Welcome back, Dream Spex member" description="Your eyewear journey, benefits, and account details in one place." /><div className="mt-10 grid gap-6 lg:grid-cols-[1.15fr_.85fr]"><section className="rounded-[28px] border border-cyan-400/30 bg-cyan-400/10 p-8"><div className="flex items-start justify-between gap-4"><div><p className="text-sm uppercase tracking-[0.3em] text-cyan-300">Account summary</p><h2 className="mt-3 text-3xl font-semibold text-white">Your private eyewear edit</h2><p className="mt-3 text-sm leading-7 text-slate-300">Manage your orders, wishlist, wallet, and preferences from here.</p></div><UserRound className="h-8 w-8 text-cyan-300" /></div><div className="mt-8 grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-xs text-slate-400">Membership</p><p className="mt-2 font-semibold text-white">Platinum member</p></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-xs text-slate-400">Wallet balance</p><p className="mt-2 font-semibold text-white">$0.00</p></div><div className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-xs text-slate-400">Wishlist</p><p className="mt-2 font-semibold text-white">{wishlist.length} saved</p></div></div></section><aside className="rounded-[28px] border border-white/10 bg-white/5 p-8 backdrop-blur-xl"><p className="text-sm uppercase tracking-[0.3em] text-cyan-300">Quick actions</p><div className="mt-5 grid gap-3"><Link to="/orders" className="flex items-center justify-between rounded-2xl border border-white/10 bg-slate-950/50 p-4 text-sm font-semibold text-white transition hover:border-cyan-400/40"><span className="flex items-center gap-3"><Package className="h-5 w-5 text-cyan-300" /> Orders</span><ArrowRight className="h-4 w-4 text-slate-500" /></Link><Link to="/wallet" className="flex items-center justify-between rounded-2xl border border-white/10 bg-slate-950/50 p-4 text-sm font-semibold text-white transition hover:border-cyan-400/40"><span className="flex items-center gap-3"><WalletCards className="h-5 w-5 text-cyan-300" /> Wallet</span><ArrowRight className="h-4 w-4 text-slate-500" /></Link><Link to="/wishlist" className="flex items-center justify-between rounded-2xl border border-white/10 bg-slate-950/50 p-4 text-sm font-semibold text-white transition hover:border-cyan-400/40"><span className="flex items-center gap-3"><Heart className="h-5 w-5 text-cyan-300" /> Wishlist ({wishlist.length})</span><ArrowRight className="h-4 w-4 text-slate-500" /></Link></div></aside></div><div className="mt-8 rounded-[28px] border border-white/10 bg-white/5 p-8"><div className="flex items-center justify-between"><h2 className="text-xl font-semibold text-white">Recent orders</h2><Link to="/orders" className="text-sm font-semibold text-cyan-300">View all</Link></div><p className="mt-6 text-sm text-slate-400">{cart.length ? 'Your current bag is ready for checkout.' : 'Your recent orders will appear here after your first purchase.'}</p></div></AccountShell>;
}

export function OrdersPage() {
  return <AccountShell><SectionTitle eyebrow="My orders" title="Your order history" description="Review purchases, payment status, and delivery progress." /><EmptyState icon={Package} title="No orders yet" copy="Your completed Dream Spex purchases will appear here with their product details and delivery status." to="/shop" action="Explore eyewear" /></AccountShell>;
}

export function WalletPage() {
  const [balance, setBalance] = useState(0);
  const [amount, setAmount] = useState('');
  const addMoney = (event: React.FormEvent) => { event.preventDefault(); const value = Number(amount); if (value > 0) { setBalance((current) => current + value); setAmount(''); } };
  return <AccountShell><SectionTitle eyebrow="Wallet" title="Your Dream Spex wallet" description="Keep your eyewear purchases and account credits in one place." /><div className="mt-10 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><section className="rounded-[28px] border border-cyan-400/30 bg-cyan-400/10 p-8"><WalletCards className="h-8 w-8 text-cyan-300" /><p className="mt-8 text-sm text-slate-300">Current balance</p><p className="mt-2 text-5xl font-semibold text-white">${balance.toFixed(2)}</p><form onSubmit={addMoney} className="mt-8 flex gap-2"><input value={amount} onChange={(event) => setAmount(event.target.value)} type="number" min="1" step="1" placeholder="Amount" aria-label="Amount to add" className="min-w-0 flex-1 rounded-full border border-white/10 bg-slate-950/60 px-4 py-3 text-sm text-white outline-none" /><Button type="submit"><Plus className="mr-2 h-4 w-4" />Add money</Button></form></section><section className="rounded-[28px] border border-white/10 bg-white/5 p-8"><h2 className="text-xl font-semibold text-white">Transaction history</h2><p className="mt-6 text-sm text-slate-400">Wallet credits and debits will appear here when transactions are available.</p></section></div></AccountShell>;
}

export function WishlistPage() {
  const { wishlist, removeFromWishlist, addToCart } = useShop();
  return <AccountShell><SectionTitle eyebrow="Wishlist" title="Saved pieces for later" description="Keep your favourite frames in one place and revisit them anytime." />{wishlist.length === 0 ? <EmptyState icon={Heart} title="Your wishlist is empty" copy="Explore the collection and save the frames that catch your eye." to="/shop" action="Browse frames" /> : <div className="mt-8 grid gap-6 lg:grid-cols-2">{wishlist.map((product) => <article key={product.id} className="rounded-[24px] border border-white/10 bg-white/5 p-5 backdrop-blur-xl"><div className="flex gap-5"><img src={product.image} alt={product.name} className="h-28 w-28 rounded-2xl object-cover" /><div className="min-w-0 flex-1"><p className="text-xs uppercase tracking-[0.25em] text-cyan-300">{product.category}</p><h2 className="mt-2 text-xl font-semibold text-white">{product.name}</h2><p className="mt-2 text-lg font-semibold text-white">${product.price}</p></div></div><div className="mt-5 flex gap-3"><button type="button" onClick={() => { addToCart(product); removeFromWishlist(product.id); }} className="flex-1 rounded-full bg-gradient-to-r from-cyan-400 to-blue-600 px-4 py-3 text-sm font-semibold text-white">Add to cart</button><button type="button" onClick={() => removeFromWishlist(product.id)} className="rounded-full border border-white/10 px-4 py-3 text-sm font-semibold text-slate-300">Remove</button></div></article>)}</div>}</AccountShell>;
}

export function AccountPage() {
  const [saved, setSaved] = useState(false);
  return <AccountShell><SectionTitle eyebrow="Account settings" title="Your account details" description="Keep your contact information and preferences up to date." /><form onSubmit={(event) => { event.preventDefault(); setSaved(true); }} className="mx-auto mt-10 max-w-3xl rounded-[28px] border border-white/10 bg-white/5 p-8 backdrop-blur-xl"><div className="grid gap-5 sm:grid-cols-2">{[['name', 'Name', 'Alicia V.'], ['email', 'Email', 'alicia@example.com'], ['phone', 'Phone', '+971 4 555 0199'], ['address', 'Address', 'Dubai, UAE']].map(([id, label, placeholder]) => <label key={id} className="text-sm text-slate-300"><span>{label}</span><input id={id} defaultValue={id === 'name' ? undefined : placeholder} placeholder={placeholder} className="mt-2 w-full rounded-full border border-white/10 bg-slate-950/60 px-4 py-3 text-sm text-white outline-none" /></label>)}</div><div className="mt-6 flex items-center gap-3"><Settings className="h-5 w-5 text-cyan-300" /><h2 className="font-semibold text-white">Security</h2></div><Link to="/forgot-password" className="mt-4 inline-flex text-sm font-semibold text-cyan-300">Change password</Link><Button type="submit" fullWidth className="mt-8">Save changes</Button>{saved ? <p className="mt-4 flex items-center justify-center gap-2 text-sm text-cyan-300"><CheckCircle className="h-4 w-4" /> Changes saved</p> : null}</form></AccountShell>;
}
